import { Calendar } from '../components/Calendar/Calendar';

export const Page = () => {
    return (        
        <Calendar calendarWidth={220} inputWidth={180} mainColor='#8a2be2'/>        
    );
}
